# Dev (recomendado): URLs relativas + proxy en vite.config.ts
VITE_API_URL=/api
VITE_BACKEND_URL=

# Alternativa sin proxy (mismo host en ambos):
# VITE_API_URL=http://127.0.0.1:8000/api
# VITE_BACKEND_URL=http://127.0.0.1:8000
# Abrir el front en http://127.0.0.1:5173 (no localhost).
